location ~ "^/api/v1/telegram(?:/(?:link|settings|test))?$" {
  proxy_pass $family_api;
  proxy_set_header Host $host;
  proxy_set_header X-Forwarded-Proto https;
  proxy_set_header X-Forwarded-For $remote_addr;
}
