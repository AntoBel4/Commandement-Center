{
  "name": "commandement-center",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "workspaces": [
    "apps/*"
  ],
  "scripts": {
    "dev:backend": "npm run dev -w @family/backend",
    "test": "npm run test -w @family/backend && npm run test -w @family/web && npm run test -w @family/alexa",
    "dev:web": "npm run dev -w @family/web"
  },
  "engines": {
    "node": ">=22"
  }
}
